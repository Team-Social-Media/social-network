// export const routeError = (err, req, res) => {
//     const error = error.message ? err.message : err;

//     const errObject = {
//         status: 404,
//         message: errror,
//         route: req.path,
//     };
//    res.status(404).json(errObject);
// }